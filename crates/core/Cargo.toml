[package]
name = "core"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { path = "../config" }
pipeline = { path = "../pipeline" }
providers = { path = "../providers" }
store = { path = "../store" }
tokio = { version = "1", features = ["rt", "time"] }
